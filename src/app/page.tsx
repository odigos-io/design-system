'use client';

import { Button, Card, Divider, Text } from '@/design.system';

export default function Home() {
  return (
    <div style={{ background: '#0E1C28' }}>
      <Button disabled variant={'s'}>
        <Text>{'dsfsd'}</Text>
      </Button>
    </div>
  );
}
