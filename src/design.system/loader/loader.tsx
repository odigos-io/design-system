import React from "react";
import { StyledLoader, LoaderWrapper } from "./loader.styled";

export function Loader() {
  return (
    <LoaderWrapper>
      <StyledLoader />
    </LoaderWrapper>
  );
}
